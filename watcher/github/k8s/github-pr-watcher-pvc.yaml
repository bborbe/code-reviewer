apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: github-pr-watcher
  namespace: '{{ "NAMESPACE" | env }}'
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
